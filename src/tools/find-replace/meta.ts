import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'Find & Replace',
  description: 'Find and replace text in bulk with plain or regex rules, capture groups and a live preview.',
  category: 'Text',
  keywords: ['find and replace', 'search replace', 'regex replace', 'bulk replace', 'batch', 'text replace', 'cari ganti', 'ganti kata', 'ganti teks'],
  symbol: 'Fr',
  icon: 'search',
}
