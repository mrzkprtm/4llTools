import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'Central Limit Theorem Simulator',
  description: 'Draw samples from skewed distributions and watch their averages settle into a normal curve.',
  category: 'Math',
  keywords: ['CLT', 'sampling distribution', 'sample mean', 'statistik', 'teorema limit pusat'],
  symbol: 'Clt',
  icon: 'presentation-chart',
}
