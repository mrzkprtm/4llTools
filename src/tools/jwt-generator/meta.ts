import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'JWT Generator & Signer',
  description: 'Create and sign JSON Web Tokens with HS256, RS256 or ES256 and verify any JWT signature.',
  category: 'Security',
  keywords: ['jwt', 'json web token', 'jwt generator', 'hs256', 'rs256', 'es256', 'sign token', 'verify jwt', 'buat token', 'tanda tangan token'],
  symbol: 'Js',
  icon: 'ticket-check',
}
