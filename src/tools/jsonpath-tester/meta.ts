import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'JSONPath Tester',
  description: 'Test JSONPath expressions and filters against your JSON and see every match with its path.',
  category: 'Developer',
  keywords: ['jsonpath', 'json query', 'json path evaluator', 'jq', 'filter json', 'json pointer', 'uji jsonpath', 'cari data json'],
  symbol: 'Jp',
  icon: 'search',
}
