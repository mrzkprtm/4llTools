import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'ULID, NanoID & UUID v7 Generator',
  description: 'Generate time-sortable UUID v7 and ULID, NanoID or UUID v4 ids in bulk and decode their timestamps.',
  category: 'Developer',
  keywords: ['uuid v7', 'ulid', 'nanoid', 'cuid2', 'sortable id', 'unique id', 'primary key', 'buat id unik', 'generator id'],
  symbol: 'Ul',
  icon: 'tickets',
}
