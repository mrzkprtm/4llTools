import type { ToolMeta } from '../types'

export const meta: ToolMeta = {
  name: 'Docker Run to Compose',
  description: 'Convert docker run commands to a docker-compose.yml file, or compose services back to docker run.',
  category: 'Developer',
  keywords: ['docker', 'docker run', 'docker compose', 'compose.yaml', 'container', 'konversi docker', 'kontainer', 'composerize'],
  symbol: 'Dc',
  icon: 'ship',
}
